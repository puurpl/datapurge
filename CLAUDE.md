# CLAUDE.md — DataPurge Project Instructions

## Project Overview

DataPurge is an open-source tool that helps people exercise their privacy rights
by automating data broker opt-out requests. It's a static site deployed on
Cloudflare Pages — all PII stays in the user's browser.

## Architecture

The web UI is a vanilla JS single-page app (ES modules, no framework, no build tooling).
A Python `build.py` script exports broker YAMLs and templates into JSON for the frontend.

```
datapurge/
├── brokers/          # YAML broker definitions (the core asset)
├── templates/        # Legal email/letter templates (templates.yaml)
├── schema/           # JSON Schema for validation
├── server/           # Python backend (registry, templates, build tooling)
│   ├── registry.py   # Broker registry library
│   └── templates.py  # Template engine (jurisdiction selection logic)
├── build.py          # Exports YAML → JSON for frontend
├── web/              # Static site (Cloudflare Pages output)
│   ├── index.html    # Landing page
│   ├── app.html      # SPA shell (hash routing)
│   ├── manifest.json # PWA manifest
│   ├── sw.js         # Service worker (offline support)
│   ├── css/style.css # Design system (dark mode, responsive)
│   ├── js/
│   │   ├── app.js        # Router, views, profile management
│   │   ├── store.js      # Multi-profile localStorage (PII + progress)
│   │   ├── templates.js  # Template selection + interpolation (client-side)
│   │   ├── queue.js      # Email queue, mass BCC, location notices
│   │   └── progress.js   # Deadline tracking, verification, monitoring
│   └── data/             # Generated by build.py (committed)
│       ├── registry.json
│       └── templates.json
└── client/           # CLI client (placeholder, not yet built)
```

## Key Design Principles

- **PII never leaves the browser.** Templates have {placeholders} filled client-side.
- **The broker YAML database is the core product.** Everything else is execution against it.
- **Legal templates are thorough and assertive.** Every template includes
  consent withdrawal, disclosure demands, dual-jurisdiction coverage, and paper trails.
- **Preemptive requests are the default.** Don't wait to confirm presence — send
  deletion demands to every broker regardless. This makes coverage complete.
- **Multi-profile support.** Users can manage separate identities with independent progress.

## Template Selection Logic

The JS `selectBestTemplate()` in `web/js/templates.js` mirrors `server/templates.py`:

- EU/UK countries → `gdpr_maximum`
- California + broker has CCPA coverage → `ccpa_maximum`
- States in `STATE_LAW_PRIORITY` map → mapped state template
- Default → `us_omnibus` (or `preemptive_blanket` for mass mode)

## Adding Brokers

Broker YAML files go in `brokers/{category}/`. See `schema/broker.schema.json`.
Key fields: `id`, `optout.methods[]`, `legal`, `timing`, `scan`.

After adding/modifying brokers, run `python3 build.py` to regenerate `web/data/registry.json`.

## Store Architecture (web/js/store.js)

- All data in `localStorage` under `datapurge_profiles` key
- Each profile: `{ id, label, pii, progress, createdAt }`
- Active profile tracked in `datapurge_active_profile`
- Automatic migration from old single-profile format
- `getTemplateFields()` returns dict matching Python `UserIdentity.to_dict()` format

## Development

```bash
# Rebuild data files after editing brokers/ or templates/
pip install pyyaml && python3 build.py

# Serve locally
cd web && python3 -m http.server 8080

# Export registry as JSON (alternative)
python -c "from server.registry import BrokerRegistry; BrokerRegistry().export_json('registry.json')"
```

## When Adding New Features

- All functionality must work in a static site context (no server-side logic)
- Email-based methods are preferred because they create paper trails
- Every broker contact should include consent withdrawal language
- Templates should reference every plausibly applicable law
- Test broker YAML files against the JSON Schema before committing
- Run `python3 build.py` after any broker/template changes

## Strategic Notes

The goal is to support the thorough, well-documented exercise of privacy rights.
Every paper trail records your wishes.
Every consent withdrawal may become legally enforceable as laws change.
Volume matters — even if individual requests are ignored, the aggregate
coverage of hundreds of brokers makes your rights real.
